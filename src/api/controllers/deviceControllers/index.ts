const createDeviceController = require('./createDeviceController');
const getDevicesAllController = require('./getDevicesAllController');

module.exports = {
  createDeviceController,
  getDevicesAllController,
};
